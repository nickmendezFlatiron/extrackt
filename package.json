{
  "name": "extrackt",
  "version": "1.0.0",
  "engines": {
    "node": "6.3.1"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build && cd ..",
    "deploy": "cp -a client/build/. public/",
    "clientbuild": "npm run build && npm run deploy && echo 'Client built!'"
  },
  "homepage": "https://github.com/nickmendezFlatiron/extrackt#readme"
}
